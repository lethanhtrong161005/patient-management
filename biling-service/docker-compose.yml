version: '3.9'

services:

  billing-service:
    build:
      context: .

    container_name: billing-service
    restart: always

    ports:
    - "9001:9001"

    networks:
      - network-internal

networks:
  network-internal:
    external: true
    name: microservice-network